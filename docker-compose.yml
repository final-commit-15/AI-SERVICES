version: '3'
services:
  llm-gateway:
    build: .
    ports:
      - "8002:8000"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
      - OLLAMA_MODEL=qwen3:4b
    env_file:
      - .env